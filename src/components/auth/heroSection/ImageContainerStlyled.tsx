import styled from 'styled-components';

export const ImageContainerStlyled = styled.div`
    width:max(calc(40vh + 5%),25rem);
    border: 8px solid rgba(255, 255, 255, 0.5);
    border-radius: 8px;
  z-index: 5;
    .ant-image-img {
      border-radius: 8px;
    }
`;
