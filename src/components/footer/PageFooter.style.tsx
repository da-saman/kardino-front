import styled from 'styled-components';
import { Layout } from 'antd';

const { Footer } = Layout;

export const PageFooterStyled = styled(Footer)`
padding: 0;
margin-top: 5rem;
text-align: center;
font-family:'Roboto' !important;
font-size: 1.2rem;
background-color: transparent;
`;
